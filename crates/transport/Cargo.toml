[package]
name = "lifeline-transport"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Project Lifeline L0/L1 transport seam: the Interface/ExternalNet contract, MTU fragmentation, and lossy-link ARQ (PRD §8.4, spectrum doc §3). Depends only on proto — the node runtime lives in lifeline-engine."

[dependencies]
lifeline-proto = { path = "../proto" }
lifeline-core = { path = "../core" }
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
lifeline-proto = { path = "../proto" }
